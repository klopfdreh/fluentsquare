
{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "devDependencies": {
    "grunt": "0.4.5",
    "grunt-contrib-sass": "0.8.0",
    "grunt-contrib-watch": "0.6.1",
    "grunt-cli": "0.1.13"
  }
}